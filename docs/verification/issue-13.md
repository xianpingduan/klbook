# #13 图片识别服务后台验证

日期：2026-09-25。任务：[GitHub #13](https://github.com/xianpingduan/klbook/issues/13)。用户确认的审查基线：`55a660b`。用户尚无 OCR 账号，授权按中国大陆使用环境选择服务。

**最新验收状态：2026-09-25 用户重新保存讯飞凭据后，真实样例测试成功，本机持久记录已核对。实际凭据与样例调用条件已通过；#13 保持开放，整体验收清单待进一步核对。详情见文末。以下阶段记录中的“待验证”保留当时状态。百度真实调用仍未验证。**

## 已实现及定向验证

服务选择、使用方法、调用及保管约定见[图片识别服务管理](../ocr-service.md)。未配置默认不出网，保存不调用供应商；主动测试仅发送明确展示的合成材料。

使用公开 HTTP API、隔离真实 SQLite 与文件、真实浏览器流程；仅供应商 HTTP 边界替换为可控响应，认证及识别 URL、表单和 PNG 数据仍经真实适配器构造。授权到期测试同步推进浏览器与服务器时间，未替换内部保存／权限模块。

- API 6 项定向流程通过：家长拒绝、配置与加密凭据重启持久化、无外部自动调用、修订冲突与稳定重试、额度拦截、有限供应商重试及安全错误、停用保留在途成功结果、停用阻止后续重试、认证期间配置变化阻止图片发送、密钥缺失不覆盖、停服后结果不确定及保守估算、重启不自动重发。
- 三浏览器定向 3/3 通过：保存／密钥不回填、合成材料预览、主动测试、响应丢失重试无额外调用、刷新找回结果、未保存导航保护、管理到期清空密钥并保留其他输入。390px 与 1440px 布局检查无页面横向溢出，样例中文字可正常显示。
- 类型检查和生产构建通过；完整 API 38/38、完整三浏览器 144/144 通过。审查修正后再次执行 OCR API 6/6、三浏览器定向 3/3 及生产构建，均通过。

证据位于 `.scratch/issue-13/`；测试凭据均为合成值，不包含真实账号密钥。

## 独立审查

实现提交 `1c63dd7`、修正提交 `e00b0c0`，比较基线均为用户确认的 `55a660b`。

### Standards

初审无文档规范违规，1 项 P3 维护性建议：测试记录的字段投影与结果转换重复。已集中于 `testFields` 和 `testResult`；独立复核确认已解决，无新增问题。未解决：0 项。

### Spec

初审发现 1 项 P1：普通文字响应 `results[].words` 的官方示例使用对象，原适配仅处理数组，关闭公式时可能将成功结果误判失败。已使用官方对象形态（含位置）通过公开 API 复现失败，再修正并验证文字和位置完整保留；同时兼容参数表中的数组形态。独立复核确认已解决，无新增问题或范围扩张。未解决的实现问题：0 项。真实服务验收仍待凭据，不计为通过。

## 旧版升级及本机运行

首先只读备份现有 schema 9 家庭资料库，在隔离副本上升级至 schema 10；全部既有表记录摘要和附件字节相同，完整性检查通过，升级未产生供应商请求。

随后核验现有应用进程、端口和数据目录，短暂停服，另存一致备份到 `.scratch/issue-13/runtime-backup`，恢复同一资料库及 8787 端口。再次核对全部既有表及附件未变化；新图片识别配置默认停用、无凭据、尝试次数为零。使用原家庭 CA 校验 HTTPS 页面返回 200，匿名管理 API 返回 401；未改变证书信任、账号或家庭入口。证据：`upgrade-result.json`、`runtime-check.json`。本项没有新增真机实测。

## 真实服务：待验证

当前没有供应商账号及凭据，**未验证真实百度认证、识别质量、账单或供应商账户权限**。自动化成功不代替这项验收，#13 保持开放。

准备好账号后在本机管理页填写凭据并保存，核对测试材料后主动测试；记录供应商服务、测试时间、结果／耗时、识别文字与预期差异、后台用量。凭据与 token 不进入记录、截图或 GitHub 评论。复杂公式、手写及真实材料需在 #14 接入流程后另测。

## 2026-09-25 补充：讯飞通用文字识别

用户明确要求增加讯飞接口适配，作为 #13 的追加范围，原百度继续保留。代码审查沿用本项已确认基线 `55a660b`，增量从 `39d03b4` 起。

新增 5 项公开 API 流程：供应商凭据隔离、三位小数单价、官方已知签名与 PNG 请求／base64 结果、0.035 元计量和切换保留额度、鉴权／嵌套异常及安全错误、切换阻止在途失败重试。使用官方签名示例的固定预期值，未在测试中重算同一签名。百度原 6 项继续通过，最终完整 API 43/43 通过。浏览器流程扩展为百度→讯飞→百度→讯飞，确认凭据复用仅限本供应商、接收方说明和测试归属、英语单词间距、0.035 元配置、三项凭据到期清空。完整三浏览器 144/144 通过；审查修正后生产构建、完整 API 与三浏览器定向 3/3 再次通过。

实现提交 `d883d9b`，修正提交 `0d77e00`，两路独立复核结果：

### Standards

初审无硬性规范违规，1 项 P3：百度和讯飞 API 测试的家长鉴权及结果轮询重复。已提取为 `test/api/ocr-fixture.ts`，独立复核无新增问题。未解决：0 项。

### Spec

初审发现 1 项 P2：AJV 的 `multipleOf: 0.1` 受浮点运算影响，合法的 0.029、0.003、0.007、0.041 元被拒绝。先通过公开 API 复现，再改为局部整数厘精度校验；合法价格原值保存，超出厘精度的 0.0031 元返回 422。独立复测确认修复，无新增问题或范围扩张。未解决的实现问题：0 项；真实供应商联调仍待验。

### 升级与运行验证

从 `39d03b4` 的旧程序在隔离库创建配置、加密凭据、成功记录、用量、草稿和原图，再升级 schema 10→11：原百度设置及密文、密钥、16 分历史用量、测试内容和图片字节均保留，升级没有自动请求供应商；显式测试仍可使用原凭据，用量准确累加至 32 分。此处供应商仍为 HTTP 边界替身，证据为 `.scratch/issue-13/xfyun-upgrade-result.json`。

本机更新前核验进程、端口、数据目录及无在途测试，另存一致备份到 `.scratch/issue-13/xfyun-runtime-backup`，包含数据库、附件及存在的凭据密钥。原资料库升级至 schema 11 后，全部既有业务记录、附件、设置、加密凭据、密钥、历史测试和等值用量核验通过，无新增外部尝试。保持 8787 和原家庭 HTTPS 入口；使用原 CA 验证管理页 200、匿名管理 API 401。证据为 `.scratch/issue-13/xfyun-runtime-check.json`。

真实百度及讯飞凭据联调均未在本轮执行，#13 继续保留真实服务待验状态；本项只接入后台合成样例测试，孩子材料的识别入口仍由 #14 实现。

## 2026-09-25 讯飞首次真实鉴权排查

用户在本机保存讯飞凭据并测试后报告通用鉴权错误。本地最近 3 次记录均为鉴权失败；使用同一真实适配器构造签名、将图片请求体最小化为空 JSON 的一次诊断请求，复现 HTTP 401。安全分类结果为 `signature_unverified` 和 `invalid_key`，未输出凭据、原始响应或签名 URL，也没有发送图片。独立无凭据时间探测及签名请求的响应时间与本机相差 0～1 秒，可排除这次明显时钟偏差。诊断证据仅保留于忽略目录 `.scratch/issue-13/xfyun-auth-probe-result.json`。

官方固定签名预期值回归通过；目前证据指向供应商拒绝该 APIKey，尚不能从本地判定是复制错误、旧凭据还是应用／服务选择错误，需要用户在控制台核对。未宣称真实识别成功或账单已验证。

原页面将所有 401／403 合并为同一提示，无法区分上述原因。新增公开 API 回归先确认此行为失败，再按已知错误类别映射固定中文说明，未知响应不回显；验证 APIKey、签名和时间错误均不重试、不计本地费用，响应中夹带凭据也不会泄漏。定向 OCR API 12/12、类型检查及生产构建通过。本次没有改动签名协议、已保存凭据或资料库结构。

## 2026-09-25 讯飞真实样例验收通过

用户确认重新保存讯飞鉴权配置并发送样例测试成功。只读核对同一家庭资料库的持久记录：

- 测试 ID：`46b29506-63ac-4300-89a3-e313c42e1cb4`，供应商讯飞，合成样例 `school-v1`。
- 发起时间：北京时间 2026-09-25 22:28:29；状态 `succeeded`，一次尝试，无重试，记录耗时 901ms。
- 返回 5 行非空文字，5 行均包含位置；去除空白后，样例的语文句子、英文句子、简单算式 `12 × 3 = 36` 和科学句子均与预期片段匹配。这是固定合成样例核对，不推算一般识别准确率，也不代表复杂公式能力。
- 本地费用估算 35 厘，即 0.035 元；这是按配置计算的估算，未核对供应商账单或免费包扣减。
- 已保存结果证据：`.scratch/issue-13/xfyun-real-success.json`，只包含安全元数据及预期片段匹配标志，不含凭据或识别原文。本次核验没有再次发送请求。

#13 中实际凭据样例调用的条件已由讯飞验证通过。#13 暂保持开放：将全部清单勾选并关闭议题的操作被自动审批拒绝，尚需核对整份验收清单与范围，不能仅凭本次样例成功推断所有条件。百度保留已实现和自动化验证状态，真实百度账号调用尚未验证。#14 承接学习端接入及四科真实纸质材料的完整性、更正、候选区域、失败与耗时实测，不把本次后台样例成功代替后续验收。
