# #14 识别辅助收集验证

用户授权继续下一项，确认审查基线 `b3337ac`。任务：[GitHub #14](https://github.com/xianpingduan/klbook/issues/14)。同时补齐 #13 第 4 项业务调用启停／额度证据。

## 实现与自动化结果

实现提交 `41f0333`，审查及状态修正提交 `6654189`。类型检查、生产构建通过。首次完整公开 API 回归 49/49 通过；最终 OCR 相关接口定向 18/18 通过，包含本项新增的 6 项流程。只替换外部供应商 HTTP，未绕过实际鉴权、保存与识别模块。

新增接口覆盖：停用时保存、启用不扫旧页、主动识别与跨设备读取；新上传只识别一次、候选确认保存与原结果追溯；业务启停、重试前检查、次数／预算与手动回退；迟到结果、错误跨页引用、设备撤销；EXIF 正向副本、原图不变及重启不重发；超过 20 条业务记录后仍能正确读取最近样例测试。最后一项先复现样例被列表截断挤出的问题，再通过独立样例投影修正。

完整三浏览器首次 145/147 通过：全部 144 项既有流程通过，新增流程 Edge 通过，Firefox／WebKit 失败在测试脚本尚未等候上传控件可用就执行选图，网络轨迹确认未发起上传。测试改为先断言控件可用，未放宽识别断言或增加固定睡眠。最终 Edge／Firefox／WebKit 的后台 OCR 与学习端收集流程定向 6/6 通过。日志保存在忽略目录 `.scratch/issue-14/full-api.log`、`full-browser.log`、`final-browser.log`。

浏览器验证上传后候选不自动选中，明确采用、文字更正、保存和原识别对照、同页第二题；重试响应丢失不重复请求，不覆盖手工文字。390px 手机尺寸无横向溢出；已检查候选、详情及原识别入口截图。模拟视口不能代替真机相机、触控与真实识别质量验证。

## 代码审查

### Standards

两次独立审查。初审 0 项硬性规范违反，1 项非阻断命名建议：共享执行器仍叫 `runTest`，容易混淆样例测试与业务调用。已改为 `runRecognition`，内部记录辅助名称同步中性化；增量复核 0 项未解决问题。

### Spec

初审及增量复核均未发现实现缺陷或范围扩张。四科真实纸质材料及手机验收属于规格第 6 项，仍待完成。实现问题 0 项，待人工验收 1 项。

## 资料库升级与本机更新

在当前家庭资料库的隔离副本上演练 schema 11→12，逐表对比全部原有行（新增字段只取默认值），核对附件和配套密钥字节、设置和加密凭据、历史用量。完整性与外键检查通过；未触发供应商请求。

核对原应用进程、端口、数据路径且无在途 OCR 后，短暂停服，另存一致备份至 `.scratch/issue-14/runtime-backup`，包含数据库、附件、凭据密钥。恢复同一资料库及原 8787 服务后，上述逐表／字节检查再次通过；讯飞已保存配置和凭据保持不变，没有新增外部尝试。原家庭 CA 验证学习端及后台 HTTPS 返回 200，匿名管理 API 返回 401；家庭入口不变。证据：`.scratch/issue-14/upgrade-rehearsal.json`、`runtime-check.json`。备份和凭据文件均不进入 Git。

## 待用户真实材料验证

此项尚未验证四科真实纸质材料及手机操作，不记为通过。已有讯飞后台合成样例实测属于 #13，不能替代学习端本项实测。真实百度凭据尚未配置。

本机更新完成后，使用原家庭 HTTPS 入口和小米 14／Chrome：

1. 每科选择一张真实材料，检查原图完整、方向正确，识别结束后是否有可用文字／范围；记录耗时和失败提示。
2. 点选一道题，检查题干、图形和批改是否完整；调整框选，改一句文字，确认学科／题号并保存。另一设备核对更正内容和原识别对照；确认未选中的题没有自动收集。
3. 从同页再收集一道；重新识别后确认原来手工更正未被覆盖，只有主动确认采用才改变内容。
4. 家长停用识别后新收集一张，检查不发起识别且可手动完成；再启用后检查旧材料不会自行重识别。

记录每科实际效果，不给出推算准确率。复杂公式缺失、候选范围漏图等应记录为能力限制／需人工调整；若影响保存完整原题、调整、找回，应作为缺陷修复。
