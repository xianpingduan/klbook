# #31 后台资料整理与来源管理验证记录

日期：2026-09-23。
任务：[GitHub #31](https://github.com/xianpingduan/klbook/issues/31)。
规格：[独立布局规格](../spec-ui-separation.md)。
实施说明：[后台资料整理](../admin-materials.md)。

## 实施结果

资料页改为按已收集/草稿分组的真实分页表格，每行一个打开入口；详情在宽屏左图右表单、窄屏上下排列。资料页提供唯一上传入口，沿用批次、取消、草稿续接、原图和修订/幂等规则。来源独立表格支持新增、改名、停用、启用，并保护未提交表单。

后台资料请求附加管理授权；服务端拒绝失效的管理请求，普通学习权限保持原约定。授权到期或被撤销后，重新验证可继续原材料与表单；保存离开失败不跳走。重新编辑时隐藏上一次的已同步提示，避免误报本次修改已保存。

## 自动化

全部使用隔离的真实 SQLite、文件和 HTTP API，不使用家庭资料库作为测试数据。

- `npm run build`：类型检查和前后端构建通过。
- `npm test`：19 个 API 测试全部通过；Edge、Firefox、WebKit 各 25 个浏览器场景，共 75 项全部通过。
- 完整回归后补充“编辑后不能继续显示旧的已同步提示”断言，确认失败后修正；三个引擎中相关 12 项复测全部通过。其余 63 项沿用上述完整回归结果。
- 审查阶段提取共用离开提示后，重新构建并复测三个引擎中受影响的 30 项场景，全部通过；包括来源、题目编辑、浏览器后退、批次切换、管理到期和保存失败。
- 新增 6 个端到端场景：后台批量续接与离开保护；服务端撤销管理授权后上传/保存被拒绝；53 条资料的分组、分页与读取失败重试；来源输入、刷新、验证失败/到期和退出保护；来源重名、并发冲突及保存离开时撤权；后台上传、更正、响应丢失重试和另一学习会话找回。
- 既有来源场景补充：后台更正关联已停用来源的题目，学习端读取一致名称和备注。API 测试补充：失效管理请求返回 403，普通学习上传与保存仍成功，未授权来源管理仍被拒绝。
- 390、820、1440 CSS 像素检查无页面横向溢出；管理表格内部滚动可由键盘聚焦，窄屏可以滚动至保存按钮。实际查看了表格、详情、来源页的宽窄屏截图。

测试截图保存在 `test-results/`，定向回归与失败证据在 `.scratch/issue-31/`。视口模拟不代表真实软键盘、系统相机或 iOS 设备通过。#30 的新版真机待验证状态保持不变。

## 原址升级

从已推送版本 `4f725fb` 导出并构建旧版，用 Edge 在临时资料库创建旧题与失败待上传图片。停止旧服务后，保持同一端口、数据目录和浏览器上下文启动新版。

结果：设备凭据字符串不变，旧题完整读取结果与来源身份不变，原图字节一致；后台可以更正旧题且保留身份与收集日期，可以继续旧版暂存图片，最终恰好两道已收集题。记录见 `.scratch/issue-31/upgrade-probe.mjs` 与 `upgrade-result.json`。

## Standards

用户确认以 `4f725fb` 为基线，规范和需求两路独立审查。规范方向没有发现文档规范硬性违反，提出 1 项低优先级重复逻辑建议：题目与来源编辑器分别实现了离开对话框的生命周期及导航释放。

已提取 `useEditorLeave` 共用离开动作、原生对话框、Portal、Escape、保存成功后的导航释放；领域保存、丢弃及文案仍由各自编辑器提供。独立复核确认建议已解决，无新增问题。Standards 未解决项：0。

## Spec

独立审查逐项核对 #31 九条验收标准及父规格对应范围，没有确认的功能缺口、错误实现或范围扩张。共用离开提示的后续复核确认授权拒绝时关闭弹窗、保存失败留页、成功或放弃后先释放导航保护的行为保持一致。Spec 未解决项：0。

## 本机运行与交付状态

本机与用户手动检查均确认原路径 `%LOCALAPPDATA%\klbook\data\family.sqlite` 不存在；此前启动记录仍指向该路径，缺失原因未确认。在已检查位置中没有找到可用的家庭资料备份。

用户确认此前均为测试资料，授权新建资料库后，已在原目录创建空库并启动正式应用及既有局域网 HTTPS 网关。严格校验证书链与主机名，确认运行最新构建、API 返回未初始化，并用真实浏览器确认首次设置页可操作。用户需在网页设置家庭账号并保存恢复码；本次没有恢复旧测试记录。记录见 `.scratch/issue-31/runtime-check.json`。未修改防火墙或系统证书信任。

#31 的实现、自动化、原址升级与双路审查完成，可关闭任务。#30 的新版真机验证仍待用户执行；下一开发项为 #32。
