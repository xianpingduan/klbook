# #8 学科与学习阶段验证

日期：2026-09-23。任务：[GitHub #8](https://github.com/xianpingduan/klbook/issues/8)。用户确认审查基线：`1934699740c347aa2d687cec1a9ad2930053e6b8`。

沿用已确认测试边界：公开 API、完整浏览器流程、隔离真实 SQLite 与实际图片。操作规则见 [学科与学习阶段](../study-archive.md)。

## 定向验证

- 第一条公开 API 测试先在新增学科接口缺失处失败，再实现通过；验证自定义学科、未配置阶段、默认值变化、旧草稿保留、显式冻结阶段、补收更正、收集日期不变、第二会话与原件字节。
- 组合查找测试先在不支持筛选参数处失败，再实现通过；覆盖六类条件、未设置、日期含起点／不含终点、停用来源可查、51条命中记录跨页不重不漏及清空恢复。
- 权限及重试测试验证未登录、无家长授权、授权跨设备或到期被拒绝，稳定重试不重复，旧修订不覆盖，重复学科、空白年级及无效学年／学期被拒绝。
- 浏览器主流程先在缺少管理入口处失败，再接入后台配置、两张图片同批暂存、换默认阶段后刷新续接、旧题更正和组合查找。390/820/1440 三视口检查无页面级横向溢出。
- 浏览器保护流程验证未保存设置离开确认、新增与保存响应丢失后重试、随后修改再保存、管理授权失效重新验证、另一设备更改设置时冲突保留输入与重新读取。
- 两条浏览器流程在 Edge、Firefox、WebKit 均取得通过结果。Firefox 首次运行因测试未等待选图控件就绪，提前注入文件未触发收集；增加可用性等待后定向复验通过。构建、类型检查与3项新增API通过。

## 原址升级与本机预览

隔离环境从基线旧版建立题目、来源、共享原文、纸质答案和待上传图片，保持地址、实际目录及浏览器凭据切换新版。旧操作重放仍有效，原件字节、关联、来源及日期不变；设置新默认阶段后，旧题和旧版待上传图片仍为未设置。记录：`.scratch/issue-8/upgrade-result.json`。

本机预览更新前，核验进程身份并备份真实库与附件至 `.scratch/issue-8/runtime-backup`。结构8升9，逐项比较原字段记录及文件摘要，新增阶段为空；没有重建家庭资料库。局域网HTTPS证书链、主机名和最新客户端检查通过，未登录管理入口仍要求登录。记录：`.scratch/issue-8/runtime-migration.json`、`runtime-check.json`。

## 最终回归与审查

完整回归和独立 Standards／Spec 审查正在进行。#4/#30 真机待验及 #28 总体验收继续单独跟踪；本任务的桌面浏览器验证不替代真机验收。
