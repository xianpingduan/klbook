# #4 局域网拍照与批量采集验证

审查基线：用户确认的 `c8dc84a`。任务：[GitHub #4](https://github.com/xianpingduan/klbook/issues/4)。

## 实现

- 系统拍照、相册多选和电脑上传使用相同收集流程；每批独立操作标识、整体原子暂存、逐张上传、取消及重开进度。
- 设备暂存格式从单张 v1 升到批量 v2，兼容旧格式读取；按资料库、账号隔离。已有服务端资料与数据库格式不变。
- 现有方向校正、框题、学科确认、已收集找回和原图保留继续复用；新增手机尺寸与 touch pointer 事件验证。
- Caddy 2.11.4 提供只绑定指定私有地址的 HTTPS 代理；后端仍监听回环地址。提供准备、启动、核对身份后停止和限范围防火墙脚本，不自动安装根信任。
- 网关返回 502/504 时明确提示家庭电脑服务不可达；材料可继续重试。

## 已执行的独立检查

- `npm run build`：TypeScript 与前后端构建通过。
- 新增批量浏览器用例：多选三张、取消单张、刷新恢复剩余材料、另一个会话找回；坏图可跳过、拍照选择器取消提示、手机尺寸 pointer 触控框题、方向校验、停止真实后端再恢复。
- 浏览器限制所有非本地请求的流程仍可登录、收集和查阅；这是网络请求约束验证，不等于拔除真实互联网出口。
- 真实 Caddy 探针在独立临时资料库完成严格 TLS 校验，拒绝不受信 CA，验证来源限制、登录、JPEG 原图上传、框题保存、第二设备会话读取及原图字节一致。真实后端停止后网关返回 502。探针 `.scratch/verify-lan-flow.mjs`，日志 `.scratch/issue-4-lan-probe.log`。
- 本机部署入口严格 TLS 请求健康检查 200、合法来源预检 204；题目资料库沿用原目录。Caddy 在 `%LOCALAPPDATA%` 的目录发生 EXDEV；项目独立目录验证可用，没有改变目录加密设置。
- 用实际 `c8dc84a` 页面在真实浏览器生成旧版待上传暂存，再切换新版页面，验证既有会话、继续上传及原图字节保留；日志 `.scratch/issue-4-upgrade.log`。
- 网关脚本通过独立端口首次启动、停止、保留证书后重启；占用端口时准确拒绝，未误报启动成功。就绪检查要求本次 PID 正在监听指定地址及端口。日志 `.scratch/issue-4-gateway-scripts.log`。
- 首次全量浏览器回归出现来源表单首次加载的输入时序问题；失败轨迹中没有发出管理写入请求，表单内容在加载状态变化后为空。修正初始 busy 状态后，WebKit 来源管理用例连续 8 次通过；增加填写与失败后输入值断言。日志 `.scratch/issue-4-source-fixed.log`。

## 最终自动验证与审查

2026-09-20：构建、类型检查通过。17 个 API 用例全部通过；修复上述输入时序问题后，最终 Edge、Firefox、WebKit 各 8 个浏览器用例全部通过，共 24 个。API 使用部署数据父目录内的独立测试资料库，浏览器使用独立临时资料库，均不访问用户题目。日志为 `.scratch/issue-4-full-test.log`（首次全量，保留失败证据）和 `.scratch/issue-4-browser-final.log`（修复后浏览器全部通过）。

两路审查基线为 `c8dc84a`，实现提交 `8187694`，修复提交 `33ff3cf`。首次 Standards 与 Spec 均发现“启动就绪误报”和“伪造本地 502 测试超出约定边界”；已核对监听进程并移除该替身。复核后两轴未解决代码发现均为 0。真实设备与实际断网结果见下节，未执行项继续保留待验收。

## 真机验收进展

### 2026-09-25新版小米Chrome补测

小米14、Android16、Chrome151.0.7922.71已完成本轮拍照、方向和触控框题、批量3张取消1张并刷新续接、取消拍照后相册单选及草稿继续整理。实际停服时手机提示无法连接且任务保留，恢复重试后电脑只新增1题，无重复。Chrome断外网由用户选择本轮跳过，其他设备、相机权限拒绝或入口不可用及本轮实际图片格式仍待补充。详见[本轮记录](device-validation-2026-09-25.md)。#4保持开放，以下2026-09-20内容为历史记录。

### 2026-09-20历史记录

2026-09-20 用户在小米 14、Android 16 完成以下实际操作并反馈通过；各浏览器覆盖范围见下表：

- 基础流程：安装证书、打开 HTTPS 应用并登录、拍照收集，在电脑端刷新找回。用户反馈“三步已经完成，暂没有错误”。
- 批量流程：相册选择 3 张，完成第一张、取消第二张、刷新页面后完成第三张，电脑刷新后只新增 2 道题。用户反馈“全部正常，电脑只新增 2 道题”。
- 图片方向与触控：用户确认“方向正常，手指框顺畅”。
- 取消拍照与相册单选：点击拍照后取消，确认页面提示，再从相册选择 1 张，完成框题、保存并在电脑端找回；用户反馈“已验证通过”。
- 真实外网断开：按验证步骤保持路由器、家庭 Wi-Fi 和电脑运行，只断开互联网连接并关闭手机移动数据，使用原局域网入口重新登录、查看已有题并收集 1 道题；用户反馈“已验证，正常”。本项为用户实际断网操作反馈，电脑及局域网服务始终可用，不代表电脑停止服务或完全离线时可首次打开页面。
- 真实服务停止与恢复：用户确认手机页面已准备后，核对并暂停实际应用进程，保留 HTTPS 网关。严格证书校验下网关返回 502；用户选图后反馈“提示无法连接，材料仍保留”。随后按原配置和资料库恢复应用，严格 HTTPS 健康检查返回 200，家庭资料库仍已初始化。用户继续保留材料、重试并完成框题保存，确认“重试成功，电脑只新增 1 道题”，未重复。本机过程记录位于 `.scratch/device-validation/service-interruption.json`，网关检查分别为 `service-stopped-gateway.json`、`service-resumed-gateway.json`。

用户补充测试照片格式为 JPG（JPEG）、Chrome 版本 `151.0.7922.71`，并明确“1、2、3 两种都测试过，4、5、6 Chrome 未测试”。据此记录为：

| 实际操作 | 手机内置浏览器（版本待补） | Chrome 151.0.7922.71 |
| --- | --- | --- |
| 1. 拍照、框题保存并在电脑找回 | 通过 | 通过 |
| 2. 相册选 3 张、取消 1 张、刷新后继续，电脑只新增 2 题 | 通过 | 通过 |
| 3. 取消拍照后改用相册单选、保存并在电脑找回 | 通过 | 通过 |
| 4. 断外网后登录、查看、收集 | 通过 | 未测试 |
| 5. 电脑停服后的提示与材料保留 | 通过 | 未测试 |
| 6. 服务恢复后重试成功且只新增 1 题 | 通过 | 未测试 |

上述为用户真机反馈，Chrome 的前三项通过不延伸为后三项通过；内置浏览器版本仍待补。

证据来源为用户真机操作反馈。系统拒绝相机权限或拍照入口不可用时的反馈仍待记录；主动取消拍照通过不表示权限拒绝场景已验证。其他安卓和苹果设备未验收，桌面 Edge/Firefox/WebKit 不替代这些真机。

管理员防火墙规则已成功创建并回读；本次安卓证书安装及访问、真实外网断开时的登录/采集/查阅、电脑停止服务时的提示与材料保留，以及恢复后重试、保存和不重复均已收到上述通过反馈。家庭路由器固定地址仍待确认，设备信息和其他未验收场景见上文。开机自启在 #22、完整离线重开在 #9、安装包在 #26。#4 保持开放及 `ready-for-human`，不将尚未执行的验收项标记通过。后续步骤见[设备操作清单](../lan-capture.md)。

2026-09-20 晚间环境准备：已恢复后端和网关，新增仅提供公共证书的临时准备页。严格 TLS 健康检查 200，证书下载字节一致，准备页 API 与私钥路径均为 404。修正 Windows PowerShell 5.1 的中文路径 JSON 读取编码问题。电脑环境自检与上述用户真机反馈分别记录。
