# #5 一页多题与跨页排序验证

日期：2026-09-23。任务：[GitHub #5](https://github.com/xianpingduan/klbook/issues/5)。
基线：用户确认的 `ad1cffeb0af276af48a89a6c13a0166877c714c7`。
实施说明：[一页多题与跨页题目](../multi-part-collection.md)。

## 实施与测试边界

同一原始页逐题框选、单独确认学科；独立题目共用原图，不复制文件作为关联。一道题包含有序题目区，可来自同页或跨页；编辑、重排和移除不改变题目身份及收集时间，原始页保持可追溯。

继续按已批准边界，通过公开 API 与完整浏览器流程验证，使用隔离真实 SQLite 和图片附件。没有使用家庭正式资料作为测试材料，没有外部供应商调用。

## 自动化证据

- 构建与类型检查通过；新增三项 API 流程、四项浏览器场景。
- API 覆盖同页独立题目、重试幂等、另一会话修改隔离；追加图片权限/并发重试、缺附件时拒绝保存且原修订不变；跨页和同页多个区域排序/移除、非法范围和重复区域、旧客户端省略多页字段时不丢其他页、重启后按序读取及原图字节一致。
- 浏览器覆盖学习端同页继续选题、清除误选后重新框选、保存响应丢失重试；手机尺寸追加图片、未确认全部范围时不能保存离开、刷新后完整读取；后台追加上传响应丢失后刷新续接、区域重排/移除、学习端查看全部题目区及完整原图。
- 初始新功能测试确认接口/入口缺失后失败，再实现并复测。浏览器测试中修正了“学科”选择器和已有“打开错题”按钮名称，避免将测试定位错误算作产品缺陷。
- 390、820、1440 CSS 像素检查无页面级横向溢出，已查看详情宽窄屏截图。浏览器视口与 WebKit 不替代手机相机、真实触控或 iOS 验证。

22 项 API 全部通过。首次完整浏览器回归的 99 项场景为 96 通过、3 失败；新增 WebKit 测试在文件入口尚禁用时强制设入文件，没有等待设备暂存读取完成。补充入口可用检查后，新增三流程的三引擎共 9 项全部通过。

审查后新增“创建响应丢失→修改备注→更新响应也丢失→再重试”的场景，验证只新增一道题且保留最新备注。随后完整执行三引擎 102 项场景，100 通过、2 个 Firefox 流程超时。请求记录显示点击“打开错题”未发送详情请求：本机暂存未读完时内部拒绝操作，按钮却可点击。用真实 IndexedDB 写事务阻塞另一标签页读取，稳定复现后修正按钮状态及等待提示；对 `collection.spec.ts` 和 `question-parts.spec.ts` 的三引擎 21 项定向回归全部通过。最终 102 项均有通过证据，不将完整运行中的失败隐去或声称单次 102 项全绿。

记录在 `.scratch/issue-5/final-regression`、`cache-read-red`、`cache-read-regression`，截图在 `test-results/question-parts-*.png`。测试使用隔离真实数据库、附件和 IndexedDB，未替换本地存储适配器。最后按审查意见改为枚举应用已创建的存储，无固定数据库名称、版本或对象仓库结构；该场景三引擎 3 项再次通过，记录在 `storage-boundary-check`。

## 原址升级

从 `ad1cffe` 导出构建旧版，在隔离真实资料库及浏览器中留下已收集题、原图、来源、设备凭据和失败待上传图片；同地址、端口、资料目录及浏览器上下文切换到新版，未清空存储。

验证通过：旧题迁移为一个有序题目区，旧字段、身份、来源、收集日期及原图字节保持；设备凭据不变，旧版保存请求携带原操作 ID 重试仍成功；后台可更正旧题并继续旧暂存图片，最终恰好两道已收集题。脚本和结果：`.scratch/issue-5/upgrade-probe.mjs`、`upgrade-result.json`。

## Standards

以用户确认的 `ad1cffe` 为基线独立审查，累计发现 2 项 P3，均已解决：

- 初审的判断性重复代码：普通上传与追加页上传重复附件保存、重试、授权及清理流程。已统一为 `storeUpload()`，客户端也复用上传请求。
- 后续真实存储阻塞测试硬编码数据库及对象仓库名称，违背测试边界。已由应用初始化并动态枚举实际结构，避免复制建库逻辑；三引擎复验通过。

最终复审无未解决发现。

## Spec

初审发现 1 项 P2：同页创建已成功但响应丢失，用户修改备注再重试会以新操作 ID 重复建题。现先重放首次创建取得稳定身份，再更新同一道题；后续更新响应丢失也保留该身份及重试标识。独立复现、修复后复验及三引擎回归通过。`2f02085` 的读取时序修复也已复审，未发现新增规格偏差。

审查汇总：Standards 累计 2 项 P3、已解决 2 项、未解决 0 项；Spec 1 项 P2、已解决 1 项、未解决 0 项。

## 当前家庭资料库升级

2026-09-23 短暂停止已核验进程身份的应用服务，将原 SQLite 在线备份接口生成的副本与完整附件保存到本机忽略目录 `.scratch/issue-5/runtime-backup`，核对附件摘要相同后启动新版。未重建、清空家庭资料库，私有备份不提交 Git。

正式库结构从 5 升级到 6，完整性检查为 `ok`。升级前后家庭记录、题目、原始页、来源及原操作记录摘要完全一致，全部原附件字节未变；每道旧题新增一个有序题目区。

本机 `http://127.0.0.1:8787` 和原局域网 `https://192.168.3.208:8443` 可访问新版；HTTPS 使用原 CA 验证证书链及主机名，学习收集、后台资料和设备地址均返回最新页面及脚本。独立未登录浏览器仍显示登录页，没有读取家庭资料。记录：`.scratch/issue-5/runtime-migration.json`、`runtime-check.json`。

## 状态边界

完整离线重开、识别分题、共享阅读材料、关联纸质答案和回收站不在本任务内。#4/#30 的真机待验结果保持独立；本次不把历史手机反馈当作新功能已通过真机验证。

