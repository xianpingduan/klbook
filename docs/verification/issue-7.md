# #7 纸质答案验证

日期：2026-09-23。任务：[GitHub #7](https://github.com/xianpingduan/klbook/issues/7)。用户确认的代码审查基线：`1d8fac52b405b0ef8bfbbcbc003b7ca17d305cd1`。

## 测试边界与定向验证

沿用用户确认的公开 API 和完整浏览器流程，使用隔离真实 SQLite、实际图片文件及浏览器设备暂存；不查询内部表作为功能测试断言，不以本地保存替身代替实际材料。操作说明及后续引用规则见 [纸质答案](../paper-answers.md)。

- 第一条 API 场景先在缺失答案接口处失败，再实现通过：无答案收集，同页与另页后补，排序，多题共享一张答案页，解除单题关联，普通旧客户端修改保留答案，另一会话和重启后的完整读取及原图字节一致。
- 附加公开 API 场景验证权限、无效范围、重复区域、修订冲突、稳定重试与实际附件缺失。补充验证发现普通题目保存未核验答案附件，先复现错误成功再修复为 503，题目内容不被修改。
- 第一条浏览器场景先在缺失“补充纸质答案”入口处失败，再实现通过：学习端同页和另页解答区、排序、原始页追溯，同页新小题不继承答案，复用已有答案页、解除首题后另一题仍可读取原件。
- 后台流程验证未保存备注保护、上传响应丢失后刷新续接、未框选区域阻止保存后离开、保存响应丢失后更改范围重试、另一设备更改时保留解答区、管理授权失效后重新验证继续。
- 三视口 390/820/1440 检查无页面级横向溢出。图片使用现有试卷 SVG 转换的实际 PNG 文件，不是手机拍摄材料；不作为真机验收结果。

## 独立审查与修复

以 `git diff 1d8fac5...HEAD` 为固定范围，Standards 与 Spec 分别由独立代理审查。

### Standards

初审 2 项：1 项 P2 规范违反（电脑答案页重复显示相机与选图）、1 项 P3 判断性重复代码建议（答案与原文编辑器各自实现相同的未确认保存重放协议）。

修复为共用 `useTouchInput`，仅触摸设备显示相机；答案与原文编辑器共用 `useRevisionedSave`，统一稳定操作 ID、原请求重放、修订校验及放弃修改。两条实际编辑流程的响应丢失、随后再改、冲突及重新验证场景均保留。

独立复核 `91800d1`：未解决硬性规范 0 项、判断性建议 0 项；无剩余严重级别。

### Spec

初审 2 项 P2：打开答案页的 GET 等待期间仍能修改备注，随后切屏会丢失新输入；重放最初建题或上传请求时未核验后来补充的答案附件。

先增加公开 API 与浏览器失败回归，再修复：题目编辑器纳入父页面的等待状态；建题、普通保存、上传及答案保存统一核验实际返回快照的题目、原文与答案附件。缺失原件返回 503，恢复后稳定重试仍只保留原题。

独立复核 `91800d1` 并重跑其故障探针：等待期间输入禁用，缺失答案原件的建题重放返回 503；未解决 0 项，无剩余严重级别。

## 升级与测试环境验证

- 从基线 `1d8fac5` 构建旧版，在隔离真实资料库建立题目、来源、共享阅读材料与关联、已确认操作及浏览器待上传材料；同地址、同数据目录切换新版。旧账号凭据、原图字节、旧操作重试、原文引用及旧题字段保留，答案初始为空；待上传材料继续完成，旧题可从后台更正。记录：`.scratch/issue-7/upgrade-result.json`。
- 首次完整回归为 API 26/26、浏览器 113/114；唯一浏览器失败发生在 WebKit 初次导航，尚未进入业务流程。隔离对照确认相同代码在端口 5060 时直接 HTTP 返回 200、浏览器导航超时，在 52060 时二者均为 200。5060 属于浏览器禁止访问的端口，依据 [WHATWG Fetch 的端口阻止规则](https://fetch.spec.whatwg.org/#port-blocking)。记录：`.scratch/issue-7/port-result.json`。
- 测试服务的自动端口改为 49152–65535，仅端口被占用时有限重试；显式端口与正式服务配置保持原行为。修复后答案和原文的三引擎定向回归 12/12，通过新增附件重放回归后的全部 API 27/27，构建与类型检查通过。

## 本机预览更新

核心流程通过后，核验运行进程身份，短暂停服并将原库及实际附件备份到本机忽略目录 `.scratch/issue-7/runtime-backup` 后启动新版。结构从 7 升为 8，原有家庭记录、题目、共享阅读材料、区域、引用和操作记录摘要未变，附件字节一致，解答区初始为空。未重建或清空家庭资料库。

本机与原局域网 HTTPS 入口返回最新页面及脚本，证书链与主机名验证通过；独立未登录浏览器仍要求登录。记录：`.scratch/issue-7/runtime-migration.json`、`runtime-check.json`。

审查修复版本再次更新前备份到 `.scratch/issue-7/runtime-backup-review`，重启后结构仍为 8，题目、原文、答案及操作记录摘要和附件字节均未改变。记录：`.scratch/issue-7/runtime-review-update.json`；再次通过局域网 TLS、最新脚本及未登录管理入口检查。

## 最终回归与范围

最终完整回归一次通过：`npm run build`（含类型检查）、`npm run test:api` 27/27、`npm run test:e2e -- --output=.scratch/issue-7/final-regression` 114/114（Edge、Firefox、WebKit 各 38）。所有测试使用隔离资料库；答案两条浏览器场景及原有采集、共享原文、账号、管理流程一并通过。

#7 的五条验收均已覆盖；#4/#30 真机待验及 #28 总体验收保持独立，本任务不替代其验收。后续按原任务顺序为 #8“扩展学科并按学习阶段查找”。议题状态和发布提交以 GitHub 完工记录为准。
