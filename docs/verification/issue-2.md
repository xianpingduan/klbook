# #2 本地家庭登录实现验证

日期：2026-09-20。审查基线：`fe1c2d3b3b5b73f9272516028315e6a151c4934b`（用户确认的既有需求与设计文档初始提交）。

## 实现范围

已实现账号初始化、登录/注销、真实首页、设备会话保持与撤销、独立家长验证、恢复码重设密码及补领。数据与应用分目录；网页平台边界覆盖配置、API、凭据和设备暂存入口。

## 已执行的逐项验证

测试只通过公开 API 和浏览器行为判断结果，使用真实隔离 SQLite 与文件目录，不直接查询数据库表断言结果。

| 验证 | 证据 |
| --- | --- |
| 首次设置码、唯一家庭、未登录拒绝、真实首页 | `test/api/family.test.ts` |
| 数据库关闭重开，身份与普通设备会话保留；密码登录、注销 | `test/api/family.test.ts` |
| 孩子直接管理被拒；密码再验；授权绑定设备、5 分钟到期；撤销立即生效 | `test/api/admin.test.ts` |
| 恢复密码保留身份、旧会话和旧码失效；家长补领恢复码 | `test/api/recovery.test.ts` |
| Origin、凭据格式、字段校验、禁缓存、重启仍限速、会话到期 | `test/api/boundaries.test.ts` |
| 并发初始化、同码恢复、旧密码登录与恢复交错 | `test/api/concurrency.test.ts` |
| 页面初始化与保存恢复码；终止并重新启动真实 Node 进程；浏览器保持登录 | `test/e2e/family.spec.ts` |
| 两个浏览器上下文验证登录、家长权限、撤销、补领与恢复；390px 视口无横向溢出 | `test/e2e/family.spec.ts` |

浏览器初始化/重启场景阻断全部非回环网页请求，仍能加载构建资源、登录与读取首页；后端账号流程无外部服务调用。此项验证不冒充实际路由器断网或手机局域网测试。

已用 Node 24.19.0、npm 12.0.2、Edge 153 执行；依赖版本见根目录锁文件。完整测试与双路审查结果在收尾时补充。

## 当前范围之外

- Caddy/Windows 服务、可信 LAN HTTPS、实际安卓与 iOS 设备仍须在部署任务实测。
- 当前浏览器流程用桌面 Edge（含手机尺寸视口）；Firefox、WebKit 与真机矩阵随后续完整收集交付补齐，不能把尺寸模拟当成安卓/iOS 验收。
- 尚未实现相机/相册收集、暂存补传用户流程、地址切换界面、资料备份或原生安装包；分别跟踪既有任务，未将其记为通过。
- 当前变更只提交本地 Git；没有自动推送代码或发布 App。
