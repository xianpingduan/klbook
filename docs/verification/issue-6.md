# #6 共享阅读材料验证

日期：2026-09-23。任务：[GitHub #6](https://github.com/xianpingduan/klbook/issues/6)。
开始提交与用户确认的代码审查基线：`c4bb9fb65b9e336f83fe7ab5c5b3ee83933b791c`。
操作与引用规则：[共享阅读材料](../shared-reading-materials.md)。

## 实施与测试边界

小题独立保存和查找，共用单独的阅读材料身份；原文可跨页，区域可重画、补充、移除和排序。解除单题引用保留原文、原图及其他小题。创建/关联响应丢失、并发修改和管理授权失效遵循现有保存保护。

按既定边界，通过公开 API 和浏览器操作验证，使用隔离真实 SQLite、图片附件和设备暂存。未以数据库表查询、内部调用次数或本地保存替身作为自动化断言；未使用家庭正式资料作测试材料。

## 自动化

- 新增两项 API 流程：实际多页附件、共享读取、排序、独立小题、另一会话与重启读取、旧客户端省略新字段时保留引用、解除单个引用；材料权限、操作重试、修订冲突、附件缺失、拒绝无效区域和不存在引用。
- 新增两项浏览器流程：学习端新建/追加跨页原文、同页新小题选择同一材料、解除首题后另一小题仍显示原文及完整图片；后台保护未保存题目修改、创建与关联响应分别丢失后编辑重试、并发修改不覆盖、管理验证失效后保留输入并继续。
- 第一个 API 场景在接口不存在时失败，第一条浏览器流程在入口不存在时失败；实现后通过。后台验证页定位最初使用了错误标题，按现有“验证家长身份”文案纠正，未将定位错误记为产品缺陷。
- 构建和类型检查通过，24 项 API 全部通过。108 项三引擎完整浏览器回归为 107 通过、1 项 WebKit 初始导航超时（尚未进入登录及业务操作）；未改代码重跑该场景后通过。两项新增流程在三个引擎中均通过；最终 108 项均有通过结果，不将完整运行记为单次全绿。记录：`.scratch/issue-6/full-regression`、`webkit-startup-recheck`。
- 390、820、1440 CSS 像素检查无页面级横向溢出，已查看详情窄屏截图。测试图片为本地生成的真实 PNG 文件，浏览器自动化不替代真实纸质阅读材料、手机相机、触控或 iOS 验收。

## 原址升级

从 `c4bb9fb` 导出并构建旧版，使用隔离真实数据留下原有题目、来源、图片、设备凭据和失败待上传材料，再切换到新版，保持地址、资料目录和浏览器上下文。

升级验证通过：原有题目与来源未变、原图字节一致、设备凭据保留；旧保存请求与原操作标识可重试，原待上传材料可继续，旧题仍可更正，最终恰好两道已收集题。证据：`.scratch/issue-6/upgrade-probe.mjs` 和 `upgrade-result.json`。

## 当前家庭资料库升级

2026-09-23 已核对运行进程身份，短暂停服后将原数据库和实际附件保存到本机忽略目录 `.scratch/issue-6/runtime-backup`，核对备份文件摘要一致后启动新版。没有重建或清空资料库，私有备份不提交到 Git。

正式库结构从 6 升至 7，完整性检查为 `ok`；家庭记录、题目、有序题目区、原始页、来源及原操作记录的升级前后摘要一致，全部原附件字节不变，没有额外生成阅读材料。记录：`.scratch/issue-6/runtime-migration.json`。

本机 `http://127.0.0.1:8787` 与原局域网 HTTPS 入口可访问最新页面和脚本，证书链及主机名验证通过。独立未登录浏览器仍要求登录，没有使用或修改家庭学习资料进行业务测试。记录：`.scratch/issue-6/runtime-check.json`。

## 待完成

实现、自动化与本机升级已完成。独立 Standards/Spec 审查等待用户确认比较基线 `c4bb9fb`，随后完成审查修复、推送和 GitHub 完成同步。#6 保持开放；#4/#30 的真机待验及 #28 总体验收仍单独跟踪。
